# Stage 02 - Challenge 02

Use only what has already been taught: `print()`, quoted string literals, variables, and assignment.

## Requirements

1. Create a variable named `first` and assign it the string `Variables can hold text.`
2. Create a variable named `second` and assign it the string `Python can print stored values.`
3. Use exactly two `print()` calls.
4. The first `print()` must print the variable `first`.
5. The second `print()` must print the variable `second`.
6. Do not repeat either required string literal inside `print()`.
7. Do not use `input()`.
8. Do not create any other variables.
9. Do not add extra output.
10. Type the Python yourself from memory.

## Required output

```text
Variables can hold text.
Python can print stored values.
```

Write the solution in `challenge_02.py`, commit it, then tell Senpai `done`.
