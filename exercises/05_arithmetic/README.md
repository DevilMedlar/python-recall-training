# Stage 05: Core Arithmetic Operators

Stage 04 is mastered. `print()`, variables, `input()`, reassignment, exact prompts, blank lines, and `int()` conversion all remain active knowledge.

## Core arithmetic cluster

The four core arithmetic operators were learned together:

- `+` adds.
- `-` subtracts.
- `*` multiplies.
- `/` divides and produces a floating-point result.

Stage 05 used adaptive mixed practice rather than a fixed ten-drill sequence.

## Passed work

- **Challenge 01:** Passed first try, no hints. Added two converted integers.
- **Challenge 02:** Passed first try, no hints. Reused one text slot, preserved both converted integers, and added them.
- **Challenge 03:** Passed first try, no hints. Used `+`, `-`, `*`, and `/` exactly once on the same two converted inputs.
- **Mixed Proficiency Test A:** Passed first try, no hints. Mixed all four operators with three prompted inputs, one reused text slot, conversion, preserved values, a fixed header, exact output order, and a blank line.

## Result

**Stage 05 is mastered on 2026-08-13.**

No mandatory delayed recall checks follow this stage. Core arithmetic will return unpredictably in later challenges, quizzes, and tests.

The four operators got their chance under pressure and behaved. Good. 😏🐍
