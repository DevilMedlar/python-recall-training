# Stage 01: `print()` and Exact Output

## Unlocked tools

For this stage, use only:

- `print()`
- Text inside quotation marks

Do not use variables, `input()`, loops, imports, or copied solutions yet.

## How to work

Use [`work.py`](work.py) for the current challenge. Replace its contents for each new attempt. Commit and push the attempt before asking for review.

Only complete **one challenge at a time**. Later challenges remain hidden here until earlier work is reviewed, so the campaign cannot be inhaled in one sitting and forgotten by breakfast.

## Challenge 01: First output

Write a Python program that produces exactly this output:

```text
Hello, world!
```

### Requirements

- Use one `print()` call.
- Match capitalization, punctuation, and spacing exactly.
- Type the code from memory.
- Do not paste code from this page or another source.

### Submission

Save your attempt in `work.py`, run it, then commit and push it. Ask for a review after the commit is visible on GitHub.
