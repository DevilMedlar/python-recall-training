# Stage 04: Converting Text to Whole Numbers

Stage 03 is mastered. Keyboard input, variables, reuse, reassignment, exact output, prompt text, and blank lines remain active knowledge and will keep returning.

## Current concept

`input()` returns text, and `int()` converts valid whole-number text into an integer.

The ten normal Stage 04 drills are now complete. Arithmetic is still locked.

## Challenge 10: Conversion capstone

**Passed on 2026-08-11.** First try, no hints.

The capstone correctly combined a fixed header, one reused text slot, a preserved converted integer, a converted integer that was later reassigned, exact prompts, chosen output order, reuse, and a blank line.

## Delayed recall gate

Stage 04 is **not mastered yet**.

Recall Check 01 is intentionally delayed until a later calendar day so the next attempt measures memory instead of immediate repetition. The earliest unlock date is **2026-08-12**.

No arithmetic is unlocked during the wait. No recall solution should be prepared in advance.

Let it simmer, then come back without peeking. The snake gets another bite tomorrow. 🐍
